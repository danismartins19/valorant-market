import styles from './index.module.css';

type Props = {
    name: string,
    price: number,
    url: string
}

export const Weapon = ({name, price, url}: Props) =>{
    return(
        <div className={}></div>
    )
}